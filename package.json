{
  "name": "puter-android",
  "version": "1.0.0",
  "description": "Puter.com Android App via Capacitor",
  "main": "index.js",
  "scripts": {
    "build": "npm run copy && npm run sync",
    "copy": "cap copy android",
    "sync": "cap sync android",
    "open": "cap open android",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
